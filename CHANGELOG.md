# 技术更新日志

## v1.1.0 - 存储系统重大升级 (最新)

**发布日期**: 2024年
**主要变更**: 从自定义NV模块升级到标准KV存储系统

### 升级背景
- **问题发现**: 自定义NV模块与Hi3861标准系统不兼容，导致0x80003016错误码
- **解决方案**: 改用符合Hi3861规范的LiteOS KV存储系统
- **目标**: 提高系统稳定性、兼容性和可维护性

### 主要变更

#### 1. 存储系统架构升级
- ✅ **移除自定义NV模块**: 删除`src/nv`目录及相关文件
- ✅ **引入标准KV存储**: 基于LiteOS `UtilsSetValue`/`UtilsGetValue` API
- ✅ **高层封装**: 创建`kv.h`/`kv.c`模块封装底层API
- ✅ **兼容性修复**: 解决与Hi3861标准系统的冲突

#### 2. API接口更新
```c
// 旧API (已移除)
nv_result_t nv_init(nv_config_t *config);
nv_result_t nv_set_string(const char *key, const char *value);
nv_result_t nv_get_string(const char *key, char *buffer, size_t buffer_size);

// 新API
kv_result_t kv_init(void);
kv_result_t kv_set_string(const char *key, const char *value);
kv_result_t kv_get_string(const char *key, char *buffer, size_t buffer_size);
```

#### 3. 功能增强
- ✅ **断电保存验证**: 自动检测数据完整性
- ✅ **自动配置初始化**: 首次运行设置默认值
- ✅ **错误处理优化**: 完善的错误码和状态返回
- ✅ **性能优化**: 减少Flash写入次数

### 影响范围

#### 修改的文件
- **新增**: `src/kv/kv.h` - KV存储模块头文件
- **新增**: `src/kv/kv.c` - KV存储模块实现
- **修改**: `main.c` - 更新为使用KV存储系统
- **修改**: `BUILD.gn` - 添加KV模块编译配置
- **删除**: `src/nv/` - 移除自定义NV模块目录

#### 功能模块影响
- **网络模块**: WiFi配置现在使用KV存储
- **启动模块**: 断电保存验证使用KV存储
- **PHP API模块**: 配置管理使用KV存储

### 升级步骤

#### 1. 代码变更
```bash
# 删除旧NV模块
rmdir /s /q "src/applications/sample/wifi-iot/app/startup/src/nv"

# 添加新KV模块
# kv.h 和 kv.c 已自动创建
```

#### 2. 构建配置更新
BUILD.gn文件已自动更新：
```gn
sources = [
    "src/kv/kv.c",  # 新增KV存储模块
    # ... 其他文件
]

include_dirs = [
    "src/kv",  # 新增KV模块头文件路径
    # ... 其他路径
]
```

#### 3. 主程序更新
main.c文件已重构：
- 移除`#include "nv.h"`
- 添加`#include "kv.h"`
- 更新网络任务使用KV存储API
- 实现断电保存验证逻辑

### 测试验证

#### 功能测试
- ✅ KV存储初始化测试
- ✅ 断电保存验证测试
- ✅ WiFi配置读写测试
- ✅ 错误处理测试

#### 兼容性测试
- ✅ 与Hi3861标准系统兼容
- ✅ 无0x80003016错误码
- ✅ Flash分区访问正常

### 已知问题解决

#### 问题1: 0x80003016错误码
- **原因**: 自定义NV模块参数与Hi3861标准系统不兼容
- **解决方案**: 使用标准KV存储系统
- **结果**: 错误码消失，系统稳定运行

#### 问题2: 配置丢失
- **原因**: 自定义NV模块数据持久化不可靠
- **解决方案**: 使用标准KV存储的Flash持久化
- **结果**: 配置数据断电后正常保存

### 性能指标

#### 存储性能
- **键名长度**: 最大32字节 → 保持不变
- **值长度**: 最大128字节 → 保持不变
- **存储数量**: 最多50个键值对 → 保持不变
- **响应时间**: 毫秒级 → 优化提升

#### 系统稳定性
- **错误率**: 显著降低
- **兼容性**: 完全符合Hi3861标准
- **可维护性**: 大幅提升

### 向后兼容性

#### 数据迁移
- **自动处理**: 系统首次运行会自动初始化KV存储
- **无数据丢失**: 原有配置可通过Web界面重新设置
- **平滑升级**: 用户无需手动干预

#### API兼容性
- **接口相似**: 新API与旧API使用方式相似
- **渐进迁移**: 支持逐步替换旧代码
- **文档更新**: 所有相关文档已更新

## v1.0.0 - 初始版本

**发布日期**: 2024年
**功能特性**:
- ✅ 基础硬件驱动（LED、蜂鸣器、传感器）
- ✅ WiFi网络连接（AP/STA双模）
- ✅ HTTP服务器和Web界面
- ✅ 自定义NV存储系统
- ✅ 中文字符支持

## 未来计划

### v1.2.0 (规划中)
- 🔄 OLED显示模块集成
- 🔄 中文字体库完善
- 🔄 更多传感器支持
- 🔄 远程固件升级(OTA)

### v2.0.0 (长期)
- 🔄 多设备组网支持
- 🔄 云端数据同步
- 🔄 人工智能功能
- 🔄 工业级可靠性

## 维护说明

### 代码规范
- 遵循OpenHarmony编码规范
- 使用Doxygen格式注释
- 模块化设计，高内聚低耦合

### 测试要求
- 所有新功能必须包含单元测试
- 重大变更需要集成测试
- 发布前进行系统级测试

### 文档更新
- 代码变更必须更新相关文档
- API变更需要更新接口文档
- 重大升级需要更新用户手册