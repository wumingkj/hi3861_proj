# 通用串口调试模块

## 功能特性

1. **函数调用格式解析**：支持 `Buzzer_Alarm(2, 50, 100)` 格式的命令
2. **简单命令格式**：支持 `help`、`echo hello world` 等简单格式
3. **自动回显**：输入时实时显示字符，支持退格键
4. **参数验证**：自动检查参数个数和格式
5. **可扩展性**：轻松注册新的命令函数

## 使用方法

### 基本命令