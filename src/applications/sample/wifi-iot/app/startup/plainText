### 模块依赖关系

main.c (启动模块) ├── time.h (时间管理) ├── buzzer.h (蜂鸣器控制) ├── dht11.h (温湿度传感器) ├── wifi.h (网络连接) └── nv.h (数据存储)
## 核心功能

### 系统初始化流程
1. **时间库初始化** - 建立时间基准
2. **硬件初始化** - GPIO、外设配置
3. **功能模块初始化** - 各模块独立初始化
4. **任务创建** - 启动主任务和定时器

### 多任务调度
- 非阻塞式任务设计
- 精确的时间控制
- 合理的任务优先级

## 代码结构

### 主入口函数
```c
static void Main_Entry(void) {
    // 1. 基础库初始化
    Time_Init();
    led_init();
    Buzzer_Init();
    smoke_sensor_init();
    lightsense_init();
    relay_init();  // 新增继电器初始化
    dht11_init();
    
    // 3. 定时器和任务创建
    Create_Buzzer_Timer();
    Create_Main_Task();
}
```

### 主任务循环
```c
static void Main_Task(void) {
    uint32_t last_sensor = 0, last_led = 0;
    
    while (1) {
        uint32_t current = Time_GetCurrentMs();
        
        // 传感器读取 (2秒间隔)
        if (current - last_sensor >= 2000) {
            Read_Sensor_Data();
            last_sensor = current;
        }
        
        // LED控制 (100毫秒间隔)
        if (current - last_led >= 100) {
            Update_LED_State();
            last_led = current;
        }
        
        // 让出CPU时间
        Time_DelayMs(10);
    }
}
```

## 配置参数

### 时间间隔配置
```c
#define SENSOR_UPDATE_INTERVAL_MS 2000    // 传感器更新间隔
#define LED_UPDATE_INTERVAL_MS    100     // LED更新间隔
```

### 重试机制
```c
#define DHT11_RETRY_COUNT 3              // 传感器重试次数
```

## 错误处理

### 初始化错误处理
```c
nv_result_t nv_ret = nv_init(&nv_config);
if (nv_ret != NV_SUCCESS) {
    log_w("NV", "Initialization failed: %d", nv_ret);
    // 降级处理或重试
}
```

### 传感器错误处理
```c
if (!g_dht11_connected) {
    log_e("DHT11", "Sensor not available");
    // 使用默认值或跳过操作
}
```

## 性能优化

### 时间管理
- 使用时间戳避免频繁系统调用
- 合理的任务调度间隔
- 非阻塞延时设计

### 资源优化
- 静态变量减少堆分配
- 内联函数调用优化
- 最小化中断屏蔽时间

## 扩展指南

### 添加新功能模块
1. 在`src/`目录创建模块
2. 在`BUILD.gn`中添加编译配置
3. 在`main.c`中集成初始化代码
4. 在主任务中添加功能调用

### 修改任务调度
```c
// 添加新的时间间隔
uint32_t last_new_feature = 0;

// 在主循环中添加
if (Time_CheckInterval(&last_new_feature, 500)) {
    New_Feature_Handler();
}
```

## 调试技巧

### 日志输出控制
```c
// 设置不同的日志级别
log_set_level(LOG_LEVEL_DEBUG);  // 详细调试
log_set_level(LOG_LEVEL_INFO);   // 一般信息
log_set_level(LOG_LEVEL_ERROR);  // 仅错误
```

### 性能监控
```c
uint32_t start_time = Time_GetCurrentMs();
// 执行操作...
uint32_t duration = Time_GetCurrentMs() - start_time;
log_d("PERF", "Operation took %lu ms", duration);
```

## 硬件接口

### GPIO引脚分配
| 功能 | 引脚 | 备注 |
|------|------|------|
| LED | GPIO2 | 状态指示灯 |
| 蜂鸣器 | GPIO14 | PWM控制 |
| DHT11 | GPIO7 | 温湿度传感器 |

### 电源管理
- 3.3V系统电压
- 合理的功耗控制
- 外设电源管理

## 相关文件

- `main.c` - 主程序文件
- `BUILD.gn` - 构建配置文件
- `debug.h` - 调试支持头文件

[返回主文档](../../README.md)
